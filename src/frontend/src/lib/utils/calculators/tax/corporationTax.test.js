import {calculateCorporationTax, calculateNetProfit, calculateGrossProfit} from './corporationTax';
import transactions from '../../../mocks/transactions';

describe('calculateNetProfit()', () => {
  const profitsObject = calculateNetProfit(transactions);

  it('should return keys for net and gross profits', () => {
    
  })
})


describe('calculateCorporationTax()', () => {
  it('should return keys for net and gross profits', () => {

  })
})