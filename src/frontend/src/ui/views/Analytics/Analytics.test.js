import reducer from "./Analytics.reducer";
import * as actions from "./Analytics.action";
import store from '../../../db/store';

describe('Analytics', () => {
  it('sample test', async () => {
    expect(true).toBe(true);
  })
});
