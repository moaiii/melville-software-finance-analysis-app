import reducer from "./Invoices.reducer";
import * as actions from "./Invoices.action";
import store from '../../../db/store';

describe('Invoices', () => {
  it('sample test', async () => {
    expect(true).toBe(true);
  })
});
