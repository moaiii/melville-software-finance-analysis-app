import reducer from "./CreateInvoice.reducer";
import * as actions from "./CreateInvoice.action";
import store from '../../../db/store';

describe('CreateInvoice', () => {
  it('sample test', async () => {
    expect(true).toBe(true);
  })
});
